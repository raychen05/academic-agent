import numpy as np

# Define the vector size
vector_size = 1536

# Generate a random vector with values between 0 and 1
random_vector = np.random.rand(vector_size).tolist()

# Print first 10 values for example
print(random_vector)